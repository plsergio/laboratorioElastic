server.host: "0"
server.name: "dashboard"
kibana.index: ".kibana"

elasticsearch.ssl.verificationMode: '${SSL_VERIFICATION_MODE}' 
elasticsearch.ssl.certificateAuthorities: [ '${SSL_CERTIFICATE_AUTHORITIES}' ]

elasticsearch.hosts: [ '${ELASTICSEARCH_HOSTS}' ]
